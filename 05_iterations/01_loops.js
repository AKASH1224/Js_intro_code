// For 
for (let index = 0; index <= 10; index++) {
    const element = index;
    // console.log(element);
}
for(let i=0;i<=10;i++){
    const element=i;
    if(element ==5){
        // console.log("5 is best number")
    }
    // console.log(i)
}

for (let i = 0; i <=10; i++) {
    const element = i;
    // console.log(`Outer loop Value : ${i}`);
    for (let j = 0; j <=10; j++) {
      //  console.log(`inner loop value ${j} and outer value ${i}`)
    //   console.log( i + "*" + j + '=' + i*j);
    } }
 
    const arrayVal =["Superman","IronMan","BatMan"]
    for(let index=0; index <arrayVal.length; index ++){
        const element = arrayVal[index];
        // console.log(element);
     }

     for(let index =0; index <=10 ; index ++){
        if (index ==5) {
            console.log(`Dedected ${index}`)
            // break;, break statement is use tot break the iteration
            // continue continue statement is used to continue the numbers
        }
      console.log("VAlue of loops are ",index)
     }

